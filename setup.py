from setuptools import setup, find_packages

setup(
    name='Logical formula and logical table',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'pandas>=1.0.0',
    ],
)
